# This class, _BotCommands, initializes various command strings used by the bot.
class _BotCommands:
    def __init__(self):
        # Initialize the StartCommand with the value 'start'
        self.StartCommand = 'start'

        # Initialize the MirrorCommand with the value 'mirror'
        self.MirrorCommand = 'mirror'

        # Initialize the UnzipMirrorCommand with the value 'unzipmirror'
        self.UnzipMirrorCommand = 'unzipmirror'

        # Initialize the ZipMirrorCommand with the value 'zipmirror'
        self.ZipMirrorCommand = 'zipmirror'

        # Initialize the CancelMirror with the value 'cancel'
        self.CancelMirror = 'cancel'

        # Initialize the CancelAllCommand with the value 'cancelall'
        self.CancelAllCommand = 'cancelall'

        # Initialize the ListCommand with the value 'list'
        self.ListCommand = 'list'

        # Initialize the SearchCommand with the value 'search'
        self.SearchCommand = 'search'

        # Initialize the StatusCommand with the value 'status'
        self.StatusCommand = 'status'

        # Initialize the AuthorizedUsersCommand with the value 'users'
        self.AuthorizedUsersCommand = 'users'

        # Initialize the AuthorizeCommand with the value 'authorize'
        self.AuthorizeCommand = 'authorize'

        # Initialize the UnAuthorizeCommand with the value 'unauthorize'
        self.UnAuthorizeCommand = 'unauthorize'

        # Initialize the AddSudoCommand with the value 'addsudo'
        self.AddSudoCommand = 'addsudo'

        # Initialize the RmSudoCommand with the value 'rmsudo'
        self.RmSudoCommand = 'rmsudo'

        # Initialize the PingCommand with the value 'ping'
        self.PingCommand = 'ping'

        # Initialize the RestartCommand with the value 'restart'
        self.RestartCommand = 'restart'

        # Initialize the StatsCommand with the value 'stats'
        self.StatsCommand = 'stats'

        # Initialize the HelpCommand with the value 'help'
        self.HelpCommand = 'help'

        # Initialize the LogCommand with the value 'log'
        self.LogCommand = 'log'

        # Initialize the SpeedCommand with the value 'speedtest'
        self.SpeedCommand = 'speedtest'

        # Initialize the CloneCommand with the value 'clone'
        self.CloneCommand = 'clone'

        # Initialize the CountCommand with the value 'count'
        self.CountCommand = 'count'

        # Initialize the WatchCommand with the value 'watch'
        self.WatchCommand = 'watch'

        # Initialize the ZipWatchCommand with the value 'zipwatch'
        self.ZipWatchCommand = 'zipwatch'

        # Initialize the QbMirrorCommand with the value 'qbmirror'
        self.QbMirrorCommand = 'qbmirror'

        # Initialize the QbUnzipMirrorCommand with the value 'qbunzipmirror'
        self
