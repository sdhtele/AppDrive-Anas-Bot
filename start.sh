# Run the update script to ensure that the code is up-to-date.
# This line executes the update.py script using Python 3.
python3 update.py 

# Once the code is updated, this line starts the bot using Python 3 and the -m flag.
# The -m flag allows you to run a module as a script, and in this case, the "bot" module is being executed.
python3 -m bot
